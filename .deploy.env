# Fill DEPLOY_HOST with your server user@host (e.g. root@1.2.3.4)
# Adjust DEPLOY_PATH if needed. FAST=1 syncs vendor/ to skip composer on server.
DEPLOY_HOST=root@77.110.107.27
DEPLOY_PORT=22
DEPLOY_PATH=/var/www/html
DEPLOY_FAST=1

# Optional: override rsync excludes as a space-separated list
# Including big/local files here speeds up deploys
RSYNC_EXCLUDES=".git/ node_modules/ storage/framework/cache/ storage/logs/ storage/debugbar/ .env backups/ Архив.zip .DS_Store .vite/"
